import StubPage from '../components/StubPage'
export default function Profile() {
  return <StubPage title="Profile" note="Education, skills, experience, and resume upload — feeding the matching engine — will be configured here." />
}
