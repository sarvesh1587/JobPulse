import StubPage from '../components/StubPage'
export default function Insights() {
  return <StubPage title="Insights" note="Aggregate trends across your target roles and locations will surface here once real ingestion is connected." />
}
