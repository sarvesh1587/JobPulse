import StubPage from '../components/StubPage'
export default function Companies() {
  return <StubPage title="Companies" note="Per-company hiring intelligence — open roles, skill demand, verified freshness — is planned for this screen." />
}
